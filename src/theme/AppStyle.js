import styled from 'styled-components';
import { blue } from './PageStyle';

export const Background = styled.div`
  position: relative;
  background-color: ${blue};
  background-size: 60%;
  z-index: -80;

`;
